# Abstention rubric

Abstain when:
- primary metric definition is missing
- readout context is too sparse
- critique would require pretending evidence exists

Good abstention:
- explains why critique is unsafe
- points to the minimum evidence needed next
